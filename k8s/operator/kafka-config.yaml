apiVersion: v1
kind: ConfigMap
metadata:
  name: kafka-config
  namespace: kafka
  labels:
    app: kafka-operator

data:
  BOOTSTRAP_SERVER: "b-1.fevmskcluster.dygfe1.c2.kafka.eu-north-1.amazonaws.com:9094"
